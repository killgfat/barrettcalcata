{
	"name": "barrettcalcata",
	"version": "1.0.0",
	"private": true,
	"scripts": {
		"dev": "./scripts/run-pywrangler.sh dev",
		"build": "uv sync",
		"preview": "./scripts/run-pywrangler.sh deploy",
		"deploy": "npm run build && ./scripts/run-pywrangler.sh deploy"
	},
	"devDependencies": {
		"wrangler": "^4.67.0"
	},
	"cloudflare": {
		"bindings": {
			"OPENAI_API_KEY": {
				"description": "OpenAI API key for AI image recognition feature. Get it from https://platform.openai.com/api-keys"
			}
		}
	}
}
