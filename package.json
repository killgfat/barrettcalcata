{
	"name": "barrettcalcata",
	"version": "1.0.0",
	"private": true,
	"scripts": {
		"dev": "uv run pywrangler dev",
		"build": "uv sync",
		"preview": "uv run pywrangler deploy",
		"deploy": "npm run build && uv run pywrangler deploy"
	},
	"devDependencies": {
		"wrangler": "^4.67.0"
	},
	"cloudflare": {
		"bindings": {
			"OPENAI_API_KEY": {
				"description": "OpenAI API key for AI image recognition feature. Get it from https://platform.openai.com/api-keys"
			}
		}
	}
}
